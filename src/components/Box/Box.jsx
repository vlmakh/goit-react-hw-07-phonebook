import styled from '@emotion/styled';
import { layout, space, color, border, flexbox, position } from 'styled-system';

const Box = styled('div')(layout, space, color, border, flexbox, position);

export { Box };
